# Local Makefile overrides (gitignored). Copy to config.mk and edit.
#
# Monorepo siblings (defaults — uncomment only if different):
# SUMERU_ROOT = ../sumeru
# ADDONS_ROOT = ../sumeru_addons
# CONF = sumeru.conf
#
# Production:
# SUMERU_ROOT = /opt/sumeru
# ADDONS_ROOT = /opt/sumeru_addons
#
# Default database override for every make run/install/update:
# DB = sumeru_dev
